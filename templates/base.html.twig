<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
         <style>
            html, body{
                font-family: 'Nunito Sans', Helvetica, sans-serif;
                
            }
            a:hover{
                color: #20E3B2;
                text-decoration: none;
            }
            body{
                
                background: url('http://localhost:8080/build/memphis-mini.png') ;
                max-width: 80vw;
                margin: 1rem auto;
                overflow-x: hidden;
            }
            input[type="textarea"]{
                max-width: 90vw;
            }
            input[type="text"], input[type="password"], input[type="email"]{
                background-color: #F5F4F4;
                border: none;   
                border-radius: 10px;
                width:100%;
                height: 2rem;
                padding: 0 .4rem; 
                margin: 10px auto;
                text-align: left;
            }
            button{
                color: white;
                border: none;
                background: linear-gradient(356.92deg, #000000 -2.55%, #434343 90.18%);
                border-radius: 30px;
                font-size: 18px;
                padding: 10px 30px;
                margin: 30px auto;
            }
            .navbarr,.searchbar, .menu, .bar, .navbarr-brand{
                display: flex;
                align-items: center;
            }
             .navbarr-brand h1{
                 padding: 1rem;
             }
            .navbarr{
                justify-content: space-between;
                max-width: 100vw;
                margin-bottom: 3rem;
            }
            ul{
                list-style: none;
            }
            a{
                color: #000;
                text-decoration: none;
            }
            select{
                margin: 0 1rem;
            }
            
            .nav-ite{
                margin: 1rem;
            }
            .nav-lin{
                display: flex;
                align-items: center;
            }
            .nav-lin svg, .nav-lin img{
               width: 33px;
            }
            .nav-lin p {
                margin: .5rem;
            }
            .searchbar button{
                margin: 1rem;
            }
        </style>
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        {{ include('inc/navbar.html.twig')}}
        
        {% block body %}{% endblock %}

        {% block javascripts %}
        {% endblock %}
    </body>
</html>
