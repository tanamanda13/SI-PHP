{% extends 'base.html.twig'%}

{% block title %} Test Debates{% endblock %}

{% block body %}
{% if debates %}
<table class="table table-striped">
  <thead>
    <tr>
      <th>Debate title</th>
      <th>Category</th>
      <th>Author</th>
      <th>Created</th>
      <th>Votes</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  {% for debate in debates %}
  <tr>
    <td>{{debate.title}}</td>
    <td>{{debate.category}}</td>
    <td>{{debate.author}}</td>
    <td>{{debate.created}}</td>
    <td>{{debate.votes}}</td>
    <td>
      <a href="/debate/1" class="btn btn-dark">Show</a>
    </td>
  </tr>
  {% endfor %}
  </tbody>
</table>
{% else %}
<p>No debates</p>
{% endif %}
{% endblock %}